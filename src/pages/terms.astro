---
import { getCollection } from 'astro:content';
const allDocs = await getCollection('other');
const terms = allDocs.find(doc => doc.slug === 'terms');
const { Content } = await terms.render();
---

<Content />