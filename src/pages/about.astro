---
import { getCollection } from 'astro:content';
const allDocs = await getCollection('other');
const about = allDocs.find(doc => doc.slug === 'about');
const { Content } = await about.render();
---

<Content />
